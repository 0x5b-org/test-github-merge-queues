{"on":["pull_request","merge_group"],"jobs":{"placeholder":{"if":"github.event_name != 'pull_request'","runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4","with":{"fetch_depth":0}},{"run":"sleep 10"},{"run":"! git log --format=%s ${{ github.event.merge_group.base_sha }}..HEAD | grep '(conflicting)'"}]}}}